# planning.yaml

Planning configuration describes only:

- planning parameters;
- scenarios;
- task graph;
- dependencies;
- visual bands.

It must not contain issuer, client, billing or document identity data.
